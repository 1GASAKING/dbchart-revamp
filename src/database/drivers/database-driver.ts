// Single source of truth lives in the shared @dbchart/schema package.
export type {
  QueryResult,
  SchemaColumn,
  SchemaTable,
  SchemaRelationship,
  DatabaseSchema,
  IDatabaseDriver,
} from "@dbchart/schema";